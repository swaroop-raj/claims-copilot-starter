"""Initial migration placeholder.

The starter uses `db/schema.sql` for first boot so the repo is easy to inspect.
Real schema evolution should happen through Alembic revisions created from here.
"""
