# claims-copilot-starter
Claude Code starter project for a claims-grounded RAG chatbot: Streamlit -> FastAPI -> PII redaction -> authz + injection checks -> RAG (pgvector, cited) -> HITL. Scaffold + tutorial only, no business logic.
